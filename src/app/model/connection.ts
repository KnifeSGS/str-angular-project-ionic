import { User } from "./user";

export class Connection {
  id: number;
  // user1: number = new User().id;
  user1: number;
  // user2: number = new User().id;
  user2: number;
  accepted: boolean
}
